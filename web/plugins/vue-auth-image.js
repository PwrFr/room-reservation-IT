import Vue from "vue";
import VueAuthImage from "vue-auth-image";

Vue.use(VueAuthImage);

// var authHeader = "Bearer " + localStorage.getItem("auth._token.google");
// Vue.prototype.$axios.defaults.headers.common["Authorization"] = authHeader;
